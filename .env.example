# Override apenas para depuração local — ignorado no app empacotado, que sempre
# usa a URL fixa. Aponte para o `ng serve` do crm-frontend-app (porta 4200 por
# padrão) quando for testar o front localmente.
SUGGESTIONS_URL=http://localhost:4200

# Override apenas para depuração local — em produção usa o default fixo (50505).
PDV_API_PORT=50505
